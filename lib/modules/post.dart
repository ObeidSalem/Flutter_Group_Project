import 'package:flutter/cupertino.dart';

class Post extends StatelessWidget {
  late String content;
  late String auther;

  Post(this.content, this.auther);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
